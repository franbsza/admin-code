import { createTheme } from '@mui/material';

export const appTheme = createTheme({
    palette: {
        mode: 'light',
        primary: { main: "#000" },
        secondary: { main: "#b81414" },
        text: { primary : "#141313" }
    },
});